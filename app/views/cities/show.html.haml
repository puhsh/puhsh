- content_for :title do
  %title= "Users - #{@city.name}, #{@city.full_state_name}"

.row
  .small-12
    %ul.no-bullet
      - @users.each do |user|
        %li
          = link_to user.name, city_user_path(@city.slug, user.slug)

    = paginate @users
