- content_for :nav do
  = render 'shared/nav'

.row(ng-controller="#{controller.class}")

  %div(ng-repeat="post in posts")
    .small12.medium12.small-centered.columns#post.boxed
      #header
        %h2(ng-bind="post.title")
      #post-image
        %img(ng-src="{{image.url}}")
