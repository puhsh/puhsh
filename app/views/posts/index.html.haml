- content_for :title do
  %title= "Posts"

.row
  .small-12
    %ul.no-bullet
      - @cities.each do |city|
        %li
          = link_to "#{city.name}, #{city.full_state_name}", city_path(city.slug)

    = paginate @cities
